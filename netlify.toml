# Netlify configuration for Nuxt.js static site
[build]
  command = "npm run generate"
  publish = "dist"
  
[build.environment]
  NODE_VERSION = "20.19.0"

# Handle SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
