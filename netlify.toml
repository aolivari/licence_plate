# Netlify configuration for Nuxt.js static site
[build]
  command = "npm run generate"
  publish = ".output/public"
  
[build.environment]
  NODE_VERSION = "20.14.0"

# Handle SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
